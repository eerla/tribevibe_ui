import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF4A4E69); // Lighter for AppBar
  static const secondary = Color(0xFF9A8C98); // Secondary accent
  static const accent = Color(0xFF22223B); // Dark accent for text
  static const background = Color(0xFFF2E9E4);
  static const card = Colors.white;
  static const button = Color(0xFF9A8C98); // Keep button visible
  static const buttonText = Colors.white;
  static const icon = Color(0xFF4A4E69); // Use same as primary for icons
  static const text = Color(0xFF22223B); // Main text color
}
